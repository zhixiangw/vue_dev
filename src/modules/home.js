import { home as state } from '../state'
import { home as getters } from '../getters'
import { home as mutations } from '../mutations'
import { home as actions } from '../actions'

export default {
  state,
  getters,
  mutations,
  actions
}